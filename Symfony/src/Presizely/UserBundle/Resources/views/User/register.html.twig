{% extends "::layout.html.twig" %}


{% block title %} Registration{% endblock title %}

{% block body %}
	<h1>
		Resources/User/register.html.twig
	</h1>
<form action="{{ path('presizely_user_test_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>

{% endblock body %}